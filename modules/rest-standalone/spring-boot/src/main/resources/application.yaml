
server:
  port: 8080
  servlet:
#    context-path: /store
    session.cookie:
      name: auth_token
      http-only: true
      secure: true
      same-site: strict
      max-age: 86400s


info.app.version: @project.version@


spring:
  mvc.problemdetails.enabled: false
  servlet.multipart:
    max-file-size: 12MB
    max-request-size: 12MB


entrystore:
  configuration.path: file:modules/rest-standalone/spring-boot/src/main/resources/entrystore.properties
  solr.url: http://localhost:8983/solr/entrystore-core
