<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.entrystore</groupId>
		<artifactId>entrystore-rest</artifactId>
		<version>6.1-SNAPSHOT</version>
	</parent>

	<artifactId>entrystore-rest-integration-test</artifactId>
	<name>EntryStore REST Integration Tests</name>

	<build>
		<testResources>
			<testResource>
				<directory>src/test/resources</directory>
				<includes>
					<include>**/*.yaml</include>
					<include>**/*.txt</include>
					<include>**/*.xml</include>
					<include>**/*.properties</include>
					<include>**/*.json</include>
				</includes>
				<filtering>true</filtering>
			</testResource>
			<!-- Binary files (e.g. JARs) and extension-less descriptors (e.g. META-INF/services service
			     files, which have no file extension) must be copied without filtering. -->
			<testResource>
				<directory>src/test/resources</directory>
				<includes>
					<include>**/*.jar</include>
					<include>META-INF/services/**</include>
				</includes>
				<filtering>false</filtering>
			</testResource>
		</testResources>

		<plugins>
			<plugin>
				<groupId>org.codehaus.gmavenplus</groupId>
				<artifactId>gmavenplus-plugin</artifactId>
				<version>5.1.0</version>
				<configuration>
					<!-- Groovy 5 rejects the plugin's legacy 1.8 default target; compile to the
					     project's Java release like the rest of the build. -->
					<targetBytecode>${maven.compiler.release}</targetBytecode>
				</configuration>
				<executions>
					<execution>
						<goals>
							<goal>compileTests</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-failsafe-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>integration-test</goal>
							<goal>verify</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<!-- Failsafe defaults failIfNoTests to false: without this, a change to the
					     Groovy/gmavenplus setup that compiles zero specs would report a green build
					     with the whole IT suite silently skipped. -->
					<failIfNoTests>true</failIfNoTests>
					<!-- Alphabetical ordering runs all shared-app ITs before the Zzz*-prefixed
						 lifecycle-owning ITs. See ENTRYSTORE-1019. -->
					<runOrder>alphabetical</runOrder>
					<argLine>
						-Dfile.encoding=UTF-8
						-Dstdout.encoding=UTF-8
						-Dstderr.encoding=UTF-8
						-Dstdin.encoding=UTF-8
					</argLine>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>report-aggregate</id>
						<phase>verify</phase>
						<goals>
							<goal>report-aggregate</goal>
						</goals>
						<configuration>
							<title>EntryStore Test Coverage</title>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<configuration>
					<skipIfEmpty>true</skipIfEmpty>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-install-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<dependencies>

		<dependency>
			<groupId>org.entrystore</groupId>
			<artifactId>entrystore-rest-spring-boot</artifactId>
			<version>6.1-SNAPSHOT</version>
			<!-- scope should stay "compile" (default), so the Jacoco plugin can generate the test coverage for spring-boot project -->
		</dependency>

		<dependency>
			<groupId>org.spockframework</groupId>
			<artifactId>spock-core</artifactId>
			<version>2.4-groovy-5.0</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.groovy</groupId>
			<artifactId>groovy-json</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.apache.groovy</groupId>
			<artifactId>groovy-xml</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-solr</artifactId>
			<version>2.0.5</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.testcontainers</groupId>
			<artifactId>testcontainers-spock</artifactId>
			<version>2.0.5</version>
			<scope>test</scope>
		</dependency>

		<!--
			Pinned: KeycloakBaseSpec uses the no-arg KeycloakContainer(), so this version decides
			the Keycloak server image, and that image must match the CAS protocol extension jar
			checked in at src/test/resources/libs/keycloak-protocol-cas-26.5.6.jar. 4.3.1 pulls a
			newer server whose org.keycloak.protocol.oidc.utils.OAuth2Code constructor changed,
			making ZzzCasLoginIT fail with a NoSuchMethodError raised inside Keycloak.
			To upgrade, bump the bundled keycloak-protocol-cas jar to the same Keycloak version
			in the same commit.
		-->
		<dependency>
			<groupId>com.github.dasniko</groupId>
			<artifactId>testcontainers-keycloak</artifactId>
			<version>4.1.1</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.awaitility</groupId>
			<artifactId>awaitility</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>com.icegreen</groupId>
			<artifactId>greenmail</artifactId>
			<version>2.1.12</version>
			<scope>test</scope>
		</dependency>

		<!-- Use the shaded (-standalone) distribution to avoid Jetty / Jackson
			 classpath conflicts with the Spring Boot app under test. -->
		<dependency>
			<groupId>org.wiremock</groupId>
			<artifactId>wiremock-standalone</artifactId>
			<version>3.13.2</version>
			<scope>test</scope>
		</dependency>

	</dependencies>
</project>
